# Base64 encode the values stored in a Kubernetes Secret
apiVersion: v1
kind: Secret
metadata:
  name: block-driver-secret
  namespace: ibm-storage-odf
type: Opaque
data:
  USERNAME: ZnNkcml2ZXI= # echo -n "fsdriver" | base64
  PASSWORD: ZnNkcml2ZXI= # echo -n "fsdriver" | base64
