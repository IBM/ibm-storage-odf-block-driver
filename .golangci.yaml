version: "2"

linters:
  default: standard        # or "none" if you only want staticcheck
  enable:
    - staticcheck
  settings:
    staticcheck:
      checks:
        - all
        - "-G114"          # disable G114
